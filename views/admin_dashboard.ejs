<%- include('partials/header') %>

    <div class="card">
        <h2 style="color: var(--danger-color);">ADMIN PANEL</h2>
        <div style="display: flex; gap: 1rem;">
            <a href="/admin/users" class="btn" style="background: var(--text-primary); color: #000;">Manage Users</a>
            <a href="/admin/flags" class="btn" style="background: var(--text-primary); color: #000;">Manage Flags</a>
            <a href="/admin/backup" class="btn" style="background: var(--accent-color); color: #000;">Backup /
                Restore</a>
        </div>
    </div>

    <%- include('partials/footer') %>